apiVersion: v1
kind: Service
metadata:
  name: {{ include "polypass-sharing.fullname" . }}
  labels:
    {{- include "polypass-sharing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "polypass-sharing.selectorLabels" . | nindent 4 }}
