apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "polypass-sharing.fullname" . }}-test-connection"
  labels:
    {{- include "polypass-sharing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "polypass-sharing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
