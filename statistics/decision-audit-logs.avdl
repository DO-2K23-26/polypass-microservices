@namespace("com.example.statistics")
protocol DecisionAuditLog {
  record Auth {
    string user_id;
  }
  record DecisionLog {
    Auth auth;
    string action;
    string credential;
    string result;
    string timestamp;
  }
}